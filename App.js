import 'expo-router/entry';

// This file serves as the entry point for Expo Snack
// The actual app structure is located in the app/ directory
// Expo Router handles the navigation and routing

export default function App() {
  // Expo Router uses the app/ directory structure for routing
  // This component is just a wrapper for Expo Snack compatibility
  return null;
}
